Функция ПолучитьОстаток(Продукция)
	
	Результат = 0;
	
	Отбор = Новый Структура;
	Отбор.Вставить("Товар", Продукция);
	
	НайденныеЗначения = РегистрыНакопления.УчетСклада.Остатки(Дата, Отбор);
	
	Если НайденныеЗначения.Количество() > 0 Тогда 
		
		Результат = НайденныеЗначения[0].Количество;	
		
	КонецЕсли;
	
	Возврат Результат;
КонецФункции







	
		ОстатокТовара = ПолучитьОстаток(ТекСтрокаСписокМатериалов.Материал);
		 Если  ОстатокТовара  < ТекСтрокаСписокМатериалов.Количество Тогда  
			 
			 Сообщить("Не достаточно товара " + ТекСтрокаСписокМатериалов.Материал + ".");
			 Сообщить("На данном складе не хватает:"  + (ТекСтрокаСписокМатериалов.Количество - ОстатокТовара) + " ед.");
			 Отказ = Истина; 
			 
		 КонецЕсли;
